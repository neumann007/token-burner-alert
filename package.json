{
  "name": "token-burner-alert-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspace token-burner-alert build",
    "watch": "npm run --workspace token-burner-alert watch"
  }
}
